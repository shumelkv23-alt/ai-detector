# For CPU-only torch (recommended, saves ~2GB vs CUDA build):
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# Then install the rest:
# pip install -r requirements.txt

fastapi==0.115.*
uvicorn[standard]==0.32.*
python-multipart==0.0.*
pillow==11.*
httpx==0.28.*
torch>=2.5
torchvision>=0.20
transformers>=4.46
numpy==2.1.*
pydantic==2.9.*
ultralytics==8.*
openai>=1.50
python-dotenv==1.*
pytest==8.*
